<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="story"
            type="com.example.android.writeitsayithearit.model.story.Story" />

        <variable
            name="cue"
            type="com.example.android.writeitsayithearit.model.cue.Cue" />


        <variable
            name="viewmodel"
            type="com.example.android.writeitsayithearit.ui.stories.StoryViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/story_constraint_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.stories.StoryFragment">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/story_top_menu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="@dimen/padding_regular"
                android:paddingLeft="@dimen/padding_regular"
                android:paddingEnd="@dimen/padding_regular"
                android:paddingRight="@dimen/padding_regular">

                <TextView
                    style="@style/Theme.WriteItSayItHearIt.TopMenuCueCardText"
                    android:id="@+id/cue_rating"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{Integer.toString(story.rating)}"
                    tools:text="123"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintLeft_toLeftOf="parent"
                    app:layout_constraintBottom_toTopOf="@id/cue_text"
                    />


                <TextView
                    style="@style/Theme.WriteItSayItHearIt.TopMenuCueCardText"
                    android:id="@+id/cue_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{cue.text}"
                    tools:text="@string/cue_lorem_ipsum"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/cue_rating"/>

                <ImageButton
                    android:id="@+id/like_story_button"
                    style="@style/Theme.WriteItSayItHearIt.TopMenuItem"
                    android:onClick="@{() -> viewmodel.onLikeStoryClick()}"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@null"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/cue_text"
                    app:srcCompat="@drawable/ic_action_like" />


            </androidx.constraintlayout.widget.ConstraintLayout>

        </com.google.android.material.card.MaterialCardView>

        <ImageButton
            android:id="@+id/toggle_menu_button"
            android:onClick="@{() -> viewmodel.onToggleMenu()}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@null"
            app:layout_constraintEnd_toEndOf="@id/story_top_menu"
            app:layout_constraintTop_toBottomOf="@id/story_top_menu"
            app:srcCompat="@drawable/ic_arrow_downward_black_24dp" />

        <TextView
            android:id="@+id/story_text_view"
            android:text="@{story.text}"
            tools:text="@string/story_lorem_ipsum"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:padding="@dimen/padding_small"
            app:layout_constraintTop_toBottomOf="@id/story_top_menu"
            app:layout_constraintBottom_toBottomOf="parent" />


    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
